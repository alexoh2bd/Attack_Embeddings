print("Start import test", flush=True)
import torch
print("Imported torch", flush=True)
import clip
print("Imported clip", flush=True)
from datasets import load_dataset
print("Imported datasets", flush=True)
